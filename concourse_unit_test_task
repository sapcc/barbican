export DEBIAN_FRONTEND=noninteractive && export UPPER_CONSTRAINTS_FILE=https://raw.githubusercontent.com/sapcc/requirements/stable/stein-m3/upper-constraints.txt && apt-get update && apt-get install -y build-essential python-pip python3-dev git libpcre++-dev gettext libsqlite3-dev libsasl2-dev libldap2-dev libssl-dev libldap-2.4-2 memcached && pip install tox && pip install six && /etc/init.d/memcached start && git clone -b stable/stein-m3 --single-branch https://github.com/sapcc/barbican.git && cd barbican && tox -e py37
